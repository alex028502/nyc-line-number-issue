{
  "dependencies": {
    "nyc": "^13.3.0",
    "mocha": "^6.0.2"
  },
  "scripts": {
    "coverage-2": "nyc --cache false npm test",
    "coverage-1": "nyc --cache false mocha example.js",
    "test": "mocha example.js",
    "simple-test": "node negative-square-root/test.js",
    "simple-coverage": "nyc --cache npm run simple-test"
  },
  "description": "showing wrong line numbers in stack traces with nyc",
  "version": "1.0.0",
  "name": "nyc-line-number-issue"
}
